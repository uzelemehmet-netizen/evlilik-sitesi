# Cloudinary (client-side unsigned upload)
# 1) Cloudinary Dashboard -> Settings -> Upload -> Upload presets -> Add upload preset
# 2) Unsigned: ENABLE
# 3) Preset name'i aşağıya yaz
VITE_CLOUDINARY_UPLOAD_PRESET=

# Opsiyonel: Cloud name (site zaten res.cloudinary.com/dj1xg1c56 kullanıyor)
VITE_CLOUDINARY_CLOUD_NAME=dj1xg1c56

# Cloudinary (server-side signed upload) - Vercel Environment Variables
# Cloud name: URL'deki kısa isimdir -> https://res.cloudinary.com/<CLOUD_NAME>/...
# Örn: dphcgfqst
# NOT: Cloudinary panelinde API key yanında görünen "Key name" (örn: mimediaflows_...) sadece etiket/isimdir,
# env'e yazılmaz ve imza hesaplamasında kullanılmaz.
CLOUDINARY_CLOUD_NAME=
CLOUDINARY_API_KEY=
CLOUDINARY_API_SECRET=

# Admin allowlist (virgülle ayrılmış email listesi)
# Örn: uzelemehmet@gmail.com,articelikkapi@gmail.com
VITE_ADMIN_EMAILS=

# Server-side (Vercel API) Firebase Admin
# Firebase Console -> Project settings -> Service accounts -> Generate new private key
# JSON'u tek satır string olarak buraya koyun. private_key içinde \n varsa desteklenir.
FIREBASE_SERVICE_ACCOUNT_JSON=

# Server-side admin allowlist (opsiyonel). Boşsa VITE_ADMIN_EMAILS veya default adminler kullanılır.
ADMIN_EMAILS=

# Matchmaking cron/job endpoint koruması (api/matchmaking-run)
MATCHMAKING_CRON_SECRET=

# WhatsApp destek hattı (wa.me için sadece rakam önerilir)
# Örn: 905550343852
VITE_WHATSAPP_NUMBER=

# Opsiyonel: Server-side (API) WhatsApp numarası.
# Not: /api endpoint'leri build zamanı değil runtime env okur.
WHATSAPP_NUMBER=

# KYC (Kimlik doğrulama) - Server-side (Vercel /api)
# KYC sağlayıcısı webhook'u bu siteye çağırır: /api/identity-kyc-webhook
# Güvenlik için webhook çağrısında aşağıdaki secret header gönderilmelidir:
#   x-kyc-webhook-secret: <KYC_WEBHOOK_SECRET>
# Frontend'de KYC butonunu aç/kapatmak için:
# 1/true/yes => açık, boş/0 => kapalı
VITE_KYC_ENABLED=0
KYC_PROVIDER=
KYC_WEBHOOK_SECRET=

# Matchmaking üyelik / ödeme metinleri (panelde gösterilir)
# Türkiye planı: 750 TL / ay
VITE_MATCHMAKING_PRICE_TRY=750
# Endonezya planı: 250.000 IDR / ay
VITE_MATCHMAKING_PRICE_IDR=250000

# Server-side (API) fiyatlar (opsiyonel override)
# Not: VITE_ olmayan env'ler build'e gömülmez; sadece /api endpoint'leri okur.
MATCHMAKING_PRICE_TRY=750
MATCHMAKING_PRICE_IDR=250000

# Türkiye EFT/FAST bilgileri (örnek alanlar)
VITE_MATCHMAKING_TR_ACCOUNT_NAME=
VITE_MATCHMAKING_TR_IBAN=

# Endonezya şirket hesabı (SWIFT/Wise veya lokal hesap) bilgileri
VITE_MATCHMAKING_ID_ACCOUNT_NAME=
VITE_MATCHMAKING_ID_BANK_DETAILS=

# Endonezya QRIS ödeme linki veya görsel URL (Cloudinary/public)
VITE_MATCHMAKING_ID_QRIS_URL=

# Google reCAPTCHA Enterprise (v3)
# Not: Bu site key public'tir. Asıl doğrulama server-side yapılmalıdır.
VITE_RECAPTCHA_ENTERPRISE_SITE_KEY=

# Server-side verification (Cloudflare Pages Functions env)
# NOT: Bunlar Vite tarafından build'e gömülmez (VITE_ prefix yok).
RECAPTCHA_ENTERPRISE_API_KEY=
RECAPTCHA_ENTERPRISE_PROJECT_ID=
RECAPTCHA_ENTERPRISE_SITE_KEY=
RECAPTCHA_ENTERPRISE_MIN_SCORE=0.5
